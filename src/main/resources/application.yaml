spring:
  application:
    name: LovableProject


  datasource:
    url: "jdbc:mysql://localhost:32772/testDB?useSSL=false&allowPublicKeyRetrieval=true"
    username: root
    password: Nish@nt
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
